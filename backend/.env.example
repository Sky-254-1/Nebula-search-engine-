
# Database (SQLite file path or PostgreSQL URL)
DATABASE_URL=nebula.db
# Production example:
# DATABASE_URL=postgresql://nebula:nebula@localhost:5432/nebula

# JWT
JWT_SECRET=change-this-to-a-random-secret-in-production
JWT_EXPIRY_HOURS=24
JWT_EXPIRY_MINUTES=30
REFRESH_TOKEN_DAYS=30

# AI providers (auto | openai | ollama | gguf | duckduckgo)
AI_PROVIDER=auto
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-3.5-turbo
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2
GGUF_MODEL_PATH=

# Search API keys
BRAVE_API_KEY=
SERPAPI_KEY=

# Redis (optional — enables distributed cache)
REDIS_URL=
CACHE_TTL_SECONDS=300

# Rate limiting
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_TIER_BASIC=30
RATE_LIMIT_TIER_PRO=120
RATE_LIMIT_TIER_ENTERPRISE=600
RATE_LIMIT_BURST_MULTIPLIER=2

# Environment
APP_ENV=development
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173

# Storage root
STORAGE_ROOT=../storage

# Security & Auth
AUTH_COOKIE_MODE=true
COOKIE_SECURE=true
COOKIE_SAMESITE=lax
ENABLE_RBAC=true
ENABLE_REFRESH_REUSE_DETECTION=true
ENABLE_AUDIT_LOGS=true

# Brute-force protection
MAX_LOGIN_ATTEMPTS=5
LOGIN_LOCKOUT_MINUTES=15
SIGNUP_RATE_LIMIT=5
LOGIN_RATE_LIMIT=5
REFRESH_RATE_LIMIT=10

# Content Security Policy
CSP_DEFAULT_SRC="'"'self'"'"
CSP_SCRIPT_SRC="'"'self'"'"
CSP_STYLE_SRC="'"'self'"'"
CSP_IMG_SRC="'"'self'"' data:"
CSP_CONNECT_SRC="'"'self'"'"
CSP_FONT_SRC="'"'self'"'"
CSP_FRAME_SRC="'"'none'"'"
CSP_OBJECT_SRC="'"'none'"'"

# Feature Flags
ENABLE_CSRF=true
ENABLE_2FA=false
ENABLE_WEBAUTHN=false
REQUIRE_EMAIL_VERIFICATION=false

# 2FA / TOTP
TOTP_ISSUER=Nebula Search
JWT_ISSUER=Nebula Search
JWT_AUDIENCE=nebula-search-api

# Encryption Key for data at rest
ENCRYPTION_KEY=

# OAuth2 / SSO
ENABLE_OAUTH=false
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
APPLE_CLIENT_ID=
APPLE_CLIENT_SECRET=
OAUTH2_REDIRECT_BASE_URI=http://localhost:8000/api/v1/auth/oauth2
OAUTH2_FRONTEND_REDIRECT_URI=http://localhost:5173/oauth/callback

# Email/SMTP Settings
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=Nebula Search
SMTP_USE_TLS=true
EMAIL_VERIFICATION_EXPIRY_HOURS=24
PASSWORD_RESET_EXPIRY_HOURS=1

# API Key Settings
API_KEY_LENGTH=32
API_KEY_PREFIX=nb_

# Observability: Sentry
SENTRY_DSN=

# Observability: OpenTelemetry
OTEL_SERVICE_NAME=nebula-search
OTEL_EXPORTER_OTLP_ENDPOINT=

# Logging
LOG_LEVEL=INFO
LOG_JSON_FORMAT=false

# Cross-Origin Headers
CROSS_ORIGIN_EMBEDDER_POLICY=require-corp
CROSS_ORIGIN_OPENER_POLICY=same-origin
CROSS_ORIGIN_RESOURCE_POLICY=same-origin

# Crawler Settings
CRAWLER_USER_AGENT=NebulaSearch/1.0
CRAWLER_MAX_CONCURRENCY=10
CRAWLER_DEFAULT_DELAY=1.0
CRAWLER_MAX_DEPTH=3
CRAWLER_MAX_PAGES_PER_JOB=100
CRAWLER_ROBOTS_TTL=86400

# Permissions-Policy
PERMISSIONS_POLICY=geolocation=(), microphone=(), camera=()

# Indexing System Settings
INDEXING_MAX_QUEUE_SIZE=10000
INDEXING_WORKER_COUNT=2
INDEXING_MAX_RETRIES=5
INDEXING_RETRY_BASE_DELAY=5.0
INDEXING_RETRY_BACKOFF_MULTIPLIER=3.0
INDEXING_CHUNK_SIZE=1000
INDEXING_CHUNK_OVERLAP=200

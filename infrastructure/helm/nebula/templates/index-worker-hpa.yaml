{{- if .Values.indexWorker.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "nebula.fullname" . }}-index-worker
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: nebula-index-worker
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: index-worker
    app.kubernetes.io/part-of: nebula
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "nebula.fullname" . }}-index-worker
  minReplicas: {{ .Values.indexWorker.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.indexWorker.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.indexWorker.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.indexWorker.autoscaling.targetMemoryUtilizationPercentage }}
{{- end }}
apiVersion: v1
kind: Service
metadata:
  name: nebula-redis
  namespace: nebula
  labels:
    app.kubernetes.io/name: nebula-redis
    app.kubernetes.io/component: cache
    app.kubernetes.io/part-of: nebula
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
  selector:
    app.kubernetes.io/name: nebula-redis

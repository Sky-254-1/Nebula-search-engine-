# Android

Native Android project is generated by Capacitor after first sync:

```bash
cd mobile
npm install
npm run build
npx cap add android   # first time only
npx cap open android
```

Build debug APK:

```bash
cd mobile/android
./gradlew assembleDebug
# APK: android/app/build/outputs/apk/debug/app-debug.apk
```
